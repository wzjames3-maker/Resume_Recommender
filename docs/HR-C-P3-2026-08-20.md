# 低风险遗留收尾 C（2026-08-20）

> 对应建议 C：`sourcing N+1`、前端防抖/`watch(route.params.id)` 等 P3

## 1. 后端：`sourcing` N+1 消除

**位置**：`apps/hr/agents/sourcing_runner.py:199-211`

**前**：
```python
for item in recalled:
    candidate = Candidate.objects.filter(id=candidate_id, workspace_id=workspace_id).first()
    check = structured_filter(job, candidate)
```

每条召回逐条查库，N 条召回 → N+1 查询。

**后**：
```python
candidate_ids = [ ... for item in recalled ...]
candidate_map = {str(c.id): c for c in Candidate.objects.filter(id__in=candidate_ids, workspace_id=workspace_id)}
for item in recalled:
    candidate = candidate_map.get(candidate_id)
```

批量一次拉取，`structured_filter` 循环内仅内存查表。`SourcingRunnerTests` 5 例仍 200，`hr.tests` 466 → 466 OK。

## 2. 前端：`watch(route.params.id)` 

**位置**：`ui/src/views/hr/candidates/detail.vue`

- 引入 `watch`，`onMounted` 后追加：
```ts
watch(() => route.params.id, () => {
  loadDetail()
  loadResumes()
})
```
- 修复：从列表点击不同候选人（`/hr/candidates/:id` 复用同一组件）时previously 仅 `onMounted` 加载一次，导致切人仍显示旧档案。

**验证**：`vue-tsc --build` 0，`curl /admin/hr/candidates/:id` 200，手动切换候选人后详情与简历表格即时刷新。

## 3. 前端：候选人列表输入防抖

**位置**：`ui/src/views/hr/candidates/index.vue`

- `import { debounce } from 'lodash-es'`（与 `folder-tree` 一致）
- 文本筛选 `filters.name/city/skills` 原 `@change="refresh"` 改为 `watch` + `debounce(() => refresh(), 300)`，`filters.resume_database_id` 等选择类仍 `@change` 即时刷新
- 模板中三输入移除 `@change`，仅保留 `v-model` + `clearable`，由防抖 watch 驱动
- 效果：连续键入时 300ms 合并一次 `GET /hr/candidates/1/20`，减少空转；`重置` 仍立即 `refresh()`

**验证**：`vue-tsc` 0，`ruff` 0；列表页输入 `Python` 后 300ms 单次请求，网络面板无每字符风暴。

## 4. 文件

- `apps/hr/agents/sourcing_runner.py`
- `ui/src/views/hr/candidates/detail.vue`
- `ui/src/views/hr/candidates/index.vue`
- 报告：`docs/HR-C-P3-2026-08-20.md`（本文）

> C 均为 P3，已随 A→B 余量顺手收敛，不影响冻结。
