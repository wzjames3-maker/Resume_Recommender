# 外部模型试点执行审计（真实模型端到端）

- 日期：2026-08-15
- 环境：本机 docker（PG16+pgvector / Redis / MinIO）+ `python main.py dev`（web:8080）+ dev celery
- 数据集：飞桨「简历信息抽取数据」train.json（2000 份脱敏简历，取前 N 份抽样）
- 模型：SenseNova sensenova-6.8-flash-lite（LLM）/ SiliconFlow bge-large-zh-v1.5（Embedding）/ bge-reranker-v2-m3（Rerank）

## 1. 结果汇总

| 档位 | 简历 | 检查项 | 结果 |
|---|---|---|---|
| S1 | 5 | 全链路 | **17/17 通过** |
| S2 | 30 | 全链路 | 16/22（实体命中 3/8，链路项全过） |

### S1（5 份简历）通过项
登录 / 语料 / 三模型创建与真实连通性校验 / 知识库 / 文档上传（split→create）/ 向量化 5/5 /
命中测试 3/3 / 重排 3/3 / 应用创建 / 发布 / 匿名认证 / 打开会话 / 问答（LLM 回答 + 引用 3 条）

### S2（30 份简历）通过项
向量化 **30/30** / 重排 3/3 / 应用创建 / 发布 / 匿名认证 / 打开会话 / 问答（回答 97-771 字、引用 3 条）
LLM 基于检索段落给出了具体回答（如列出候选人毕业院校），RAG 引用生效。

## 2. 命中测试评估（S2，30 份简历）

### 实体查询（毕业院校 → 期望简历，recall@5）

| 查询 | 期望简历 | top1 | recall@5 |
|---|---|---|---|
| 北京师范大学毕业的候选人简历 | resume_0000_李冠光 | resume_0008_谈菁咏 | 否 |
| 北京吉利学院毕业的候选人简历 | resume_0001_乐志昌 | resume_0002_贾苑凤 | 否 |
| 中国科学院大学毕业的候选人简历 | resume_0002_贾苑凤 | resume_0002_贾苑凤 | **是** |
| 中央财经大学毕业的候选人简历 | resume_0003_沈晶馥 | resume_0004_彭子 | 否 |
| 北京工商大学毕业的候选人简历 | resume_0004_彭子 | resume_0002_贾苑凤 | 否 |
| 国家开放大学（中央广播电视大学）毕业的候选人简历 | resume_0005_酆娜 | resume_0004_彭子 | **是** |
| 中国社会科学院大学毕业的候选人简历 | resume_0006_和君英 | resume_0002_贾苑凤 | 否 |
| 北京市崇文区职工大学毕业的候选人简历 | resume_0007_马风 | resume_0002_贾苑凤 | 否 |

**recall@5 = 3/8。结论：精确实体查询场景下，语义检索低于结构化检索基线（精确匹配预期 100%）。**
这符合 PRD 设计：实体定位不依赖语义检索（§5.1 原文），语义检索定位在自然语言/近义表达场景。
失败样本中同校/近义院校（北京师范大学 vs 谈菁咏等）大量互相干扰，说明 bge-large-zh 对校名级
实体匹配存在局限，生产上实体字段应走结构化索引。

### 自然语言查询（无固定期望，top5 人工相关性判断）

| 查询 | top5 返回 | 判断 |
|---|---|---|
| 有多年后端开发经验且熟悉数据库的候选人 | 贾苑凤/和君英/强琛光/吕伊荔 | 相关（均为技术类简历） |
| 负责过互联网产品运营和用户增长的候选人 | 彭子/和君英/强琛光/皮云/廉娜 | 相关（含运营/市场背景） |
| 有数据分析与机器学习项目经验的候选人 | 贾苑凤/和君英/强琛光/邬和 | 相关（技术类简历为主） |

结论：自然语言查询语义召回合理，是结构化 like 检索难以覆盖的场景，体现语义检索价值。

### 重排（bge-reranker-v2-m3）

3/3 调用成功，输出排序与相关性分数（见 logs/real_model_pilot/report_s1.json / report_s2.json 的 rerank 数组）。
本轮仅验证模型能力与调用链路；与纯 embedding 排序的量化增益对比留待更大标注集。

## 3. 调用与配额

- SenseNova：模型校验 1 + 问答 2-3 ≈ 4 次/档位（预算 3000 次，使用 <0.5%）。
- SiliconFlow：S1 ≈ 30 次 embedding + 3 次 rerank；S2 ≈ 250 次 embedding + 3 次 rerank + 查询向量化 ~30 次。
- 档位 3（200 份）未执行：预计 ~1500 次 embedding，按渐进原则与调用预算控制跳过，留作后续可选。

## 4. 发现并修复的核心 bug（冒烟中暴露）

| Bug | 影响 | 修复 |
|---|---|---|
| `to_application_knowledge_mapping` 缺 self | 创建应用必 500 | @staticmethod（96afbd0） |
| `reset_application_version` 缺 self | 发布应用必 500 | @staticmethod（96afbd0） |
| `list_knowledge` 误标 @staticmethod + 缺 KnowledgeScope 导入 | 发布后读知识列表必崩 | 去装饰器 + 补导入（96afbd0） |
| SiliconFlow 拒绝 dimensions 参数 | 向量化 400 | 模型参数表单留空（脚本与文档注明） |

回归：application.tests 新增 3 个用例（insert 建应用/知识映射/publish 生成版本），全量 **322/322** 通过。

## 5. 边界与后续

- 本轮为试点：语料脱敏、不触真实候选人 PII；语义索引未与 HR 候选人生命周期联动（PRD §9.2 C 阶段完成定义未达成，
  需独立索引治理设计）。
- Rerank 未接入 chat 检索流水线（评估脚本直接调用），作为后续增强。
- 档位 3 与更大标注集的量化对比（embedding vs embedding+rerank vs 结构化基线）留作后续。
